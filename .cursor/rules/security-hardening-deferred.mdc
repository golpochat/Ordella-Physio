---
description: Deferred security hardening backlog — clinic backend + frontend-web
alwaysApply: true
---

# Security Hardening — Deferred Backlog

The main hardening pass (P0–P2) is **complete**. Do not re-implement finished work unless fixing a bug.

## Completed (do not redo)

- HttpOnly refresh BFF (`apps/frontend-web/app/api/auth/*`, no refresh in localStorage)
- Redis/Upstash rate limits + shared brute-force counters
- Security regression tests (`backend/test/security.spec.ts`)
- Encrypted backups + cron + restore drill (`backend/scripts/backup-*.mjs`, `pnpm backup:drill`)
- Nonce-based CSP (`apps/frontend-web/lib/security/csp.ts`, middleware nonce)

## Still open — implement when triggered

### 1. ClamAV on upload routes (P3)

**Trigger:** Clinic backend gains real file-upload endpoints.

**Already built:**

- `backend/src/modules/security/virus-scan.service.ts`
- `backend/src/modules/security/file-upload.ts` → `validateAndScanUpload()`

**Todo when uploads ship:**

- Call `validateAndScanUpload()` in the upload handler(s) before persisting files
- Set `CLAMAV_HOST` + `CLAMAV_REQUIRED=true` in production `.env`
- Add e2e test: clean file passes, EICAR/test signature rejected

### 2. Automated JWT key rotation

**Trigger:** First scheduled rotation or compliance requirement.

**Already built:** Manual rotation via `JWT_SECRET_PREVIOUS` / `JWT_REFRESH_SECRET_PREVIOUS` in `backend/src/utils/jwt.ts`.

**Todo:**

- Runbook or script to rotate secrets with overlap window
- Optional: cron/job + secret manager integration (Vault, AWS SM, etc.)
- Test that tokens signed with previous secret still verify during overlap

### 3. Minor cleanup (low priority)

- Security test noise: `logSecurityEvent` audit FK fails when `tenantId` is unknown (`audit_logs_tenantId_fkey`) — tests pass; fix audit tenant handling if persisting security events to DB

## Production checklist (before deploy)

- Replace all `change-me-*` secrets in `backend/.env` and `apps/frontend-web/.env`
- Set `REDIS_URL` (backend) and `UPSTASH_REDIS_REST_*` (frontend) for multi-instance
- Enable backup cron (`backup-run.sh`, `backup-run.ps1`, or Docker `--profile backup`)
- Run `pnpm --filter @ordella/clinic-backend backup:drill` monthly

## Key env docs

- `backend/.env.example` — full backend security + backup vars
- `apps/frontend-web/.env.example` — auth, rate limit, CSP vars
