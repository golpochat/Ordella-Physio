# Deprecated — do not use for new work

This app is **legacy**. The primary web frontend is **`apps/frontend-web`** (port 3010, Docker service `frontend` in `docker-compose.dev.yml`).

See [docs/implementation-audit-tracker.md](../../docs/implementation-audit-tracker.md) for the live platform inventory.
