http:
  routers:
    reporting-service:
      rule: PathPrefix(`/reporting-service`)
      entryPoints:
        - websecure
      service: reporting-service
      middlewares:
        - chain-default
        - circuit-breaker
      tls:
        certResolver: letsencrypt
