http:
  routers:
    api-gateway:
      rule: PathPrefix(`/api-gateway`)
      entryPoints:
        - websecure
      service: api-gateway
      middlewares:
        - chain-default
        - circuit-breaker
      tls:
        certResolver: letsencrypt
