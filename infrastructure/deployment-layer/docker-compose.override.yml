# Optional local development overrides
# Automatically merged when running: docker compose -f docker-compose.local.yml up
#
# Uncomment sections below to mount source for hot-reload during development.

services:
  api-gateway:
    # volumes:
    #   - ../../services/api-gateway/src:/app/services/api-gateway/src:ro
    environment:
      NODE_ENV: development
      LOG_LEVEL: debug

  frontend-web:
    # volumes:
    #   - ../../apps/frontend-web/src:/app/apps/frontend-web/src:ro
    environment:
      NODE_ENV: development

  auth-service:
    environment:
      NODE_ENV: development
      LOG_LEVEL: debug
