apiVersion: v1
kind: Secret
metadata:
  name: global-secret
type: Opaque
stringData:
  # PLACEHOLDER — inject via External Secrets Operator / Vault / Sealed Secrets
  JWT_SECRET: "REPLACE_VIA_STAGING_SECRET_MANAGER"
  JWT_ACCESS_SECRET: "REPLACE_VIA_STAGING_SECRET_MANAGER"
  DATABASE_URL: "REPLACE_VIA_STAGING_SECRET_MANAGER"
  REDIS_PASSWORD: "REPLACE_VIA_STAGING_SECRET_MANAGER"
  EMAIL_PROVIDER_API_KEY: "REPLACE_VIA_STAGING_SECRET_MANAGER"
  STRIPE_SECRET_KEY: "REPLACE_VIA_STAGING_SECRET_MANAGER"
