apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  labels:
    app: payment-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
        tier: backend
    spec:
      containers:
        - name: payment-service
          image: ghcr.io/ordella/payment-service:latest
          ports:
            - containerPort: 3057
          env:
            - name: PORT
              value: "3057"
          envFrom:
            - configMapRef:
                name: global-config
            - secretRef:
                name: global-secret
          readinessProbe:
            httpGet:
              path: /health
              port: 3057
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3057
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
