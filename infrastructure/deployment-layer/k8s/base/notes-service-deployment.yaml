apiVersion: apps/v1
kind: Deployment
metadata:
  name: notes-service
  labels:
    app: notes-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notes-service
  template:
    metadata:
      labels:
        app: notes-service
        tier: backend
    spec:
      containers:
        - name: notes-service
          image: ghcr.io/ordella/notes-service:latest
          ports:
            - containerPort: 3055
          env:
            - name: PORT
              value: "3055"
          envFrom:
            - configMapRef:
                name: global-config
            - secretRef:
                name: global-secret
          readinessProbe:
            httpGet:
              path: /health
              port: 3055
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3055
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
