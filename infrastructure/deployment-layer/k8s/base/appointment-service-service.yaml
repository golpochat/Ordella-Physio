apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  labels:
    app: appointment-service
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: appointment-service
  ports:
    - name: http
      port: 3054
      targetPort: 3054
