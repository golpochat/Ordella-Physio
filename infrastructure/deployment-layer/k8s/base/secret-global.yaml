apiVersion: v1
kind: Secret
metadata:
  name: global-secret
  labels:
    app.kubernetes.io/part-of: ordella-physio
type: Opaque
stringData:
  # PLACEHOLDER — replace via external secret manager or sealed-secrets in real deployments
  JWT_SECRET: "change-me-jwt-secret-min-32-characters-long"
  JWT_ACCESS_SECRET: "change-me-jwt-secret-min-32-characters-long"
  DATABASE_URL: "postgresql://physio:physio@postgres:5432/ordella?schema=public"
  REDIS_PASSWORD: "changeme-redis-password"
  EMAIL_PROVIDER_API_KEY: "changeme-email-provider-key"
  SMS_PROVIDER_API_KEY: "changeme-sms-provider-key"
  STRIPE_SECRET_KEY: "changeme-stripe-secret"
  STRIPE_WEBHOOK_SECRET: "changeme-stripe-webhook-secret"
