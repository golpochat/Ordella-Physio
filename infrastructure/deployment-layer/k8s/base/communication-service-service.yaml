apiVersion: v1
kind: Service
metadata:
  name: communication-service
  labels:
    app: communication-service
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: communication-service
  ports:
    - name: http
      port: 3058
      targetPort: 3058
