export type StaffLocationLink = {
  id: string;
  staffId: string;
  locationId: string;
};

export type StaffLocationSummary = {
  id: string;
  locationId: string;
  name: string;
  code: string;
};
